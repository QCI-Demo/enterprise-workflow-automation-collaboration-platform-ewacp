apiVersion: v1
kind: Service
metadata:
  name: {{ include "taskScheduler.fullname" . }}
  labels:
    {{- include "taskScheduler.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app.kubernetes.io/name: {{ include "taskScheduler.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
